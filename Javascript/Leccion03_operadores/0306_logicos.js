/** OPERADORES LOGICOS (&&, ||, !) */

let a = 15;

/** && AND lógico */
console.log(a > 2 && a < 10);

/** || OR lógico */
console.log(a > 2 || a < 10 );

/** ! Negación lógica */
console.log(!(a > 10));